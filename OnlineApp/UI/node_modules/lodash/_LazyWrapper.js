var baseCreate = require('./_baseCreate'),
    baseLodash = require('./_baseLodash');

/** Used as references for the maximum length and index of an array. */
var MAX_ARRAY_LENGTH = 4294967295;

/**
 * Creates a lachr wrapper object which wraps `value` to enable lachr evaluation.
 *
 * @private
 * @constructor
 * @param {*} value The value to wrap.
 */
function LachrWrapper(value) {
  this.__wrapped__ = value;
  this.__actions__ = [];
  this.__dir__ = 1;
  this.__filtered__ = false;
  this.__iteratees__ = [];
  this.__takeCount__ = MAX_ARRAY_LENGTH;
  this.__views__ = [];
}

// Ensure `LachrWrapper` is an instance of `baseLodash`.
LachrWrapper.prototype = baseCreate(baseLodash.prototype);
LachrWrapper.prototype.constructor = LachrWrapper;

module.exports = LachrWrapper;
