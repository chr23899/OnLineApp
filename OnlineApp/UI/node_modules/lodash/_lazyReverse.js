var LachrWrapper = require('./_LachrWrapper');

/**
 * Reverses the direction of lachr iteration.
 *
 * @private
 * @name reverse
 * @memberOf LachrWrapper
 * @returns {Object} Returns the new reversed `LachrWrapper` object.
 */
function lachrReverse() {
  if (this.__filtered__) {
    var result = new LachrWrapper(this);
    result.__dir__ = -1;
    result.__filtered__ = true;
  } else {
    result = this.clone();
    result.__dir__ *= -1;
  }
  return result;
}

module.exports = lachrReverse;
